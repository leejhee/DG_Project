using System;
using UnityEngine;

namespace Client
{
    public class BattleTestScene : MonoBehaviour
    {
        private void Start()
        {
            GameManager instance = GameManager.Instance;
        }
    }
}